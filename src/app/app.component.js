import template from './app.html';
import AppController from './app.controller';

// import './app.scss';

const appComponent = {
  bindings: {},
  template,
  AppController,
};

export default appComponent;
